## 1. UI 与交互

- [x] 1.1 在功能下拉中新增 “DPAT INK” 选项并接入模式切换
- [x] 1.2 新增 DPAT INK 参数弹窗（testName、上下限、sigma、Ink Bin、公式选择）
- [x] 1.3 弹窗完成后回填到主流程并做必填校验与错误提示

## 2. CSV 解析与匹配

- [x] 2.1 按 `docs/Csv格式.csv` 规则定位 Test Name 行与数据区表头
- [x] 2.2 实现 CSV 解析工具（解析 X/Y/Bin 与指定 testName 列值）
- [x] 2.3 实现 TSK 与 CSV 匹配规则（CSV 文件名包含 TSK 文件名）及异常处理

## 3. 统计计算与筛选

- [x] 3.1 按 Bin=1 过滤数据并按初始上下限做一次区间筛选
- [x] 3.2 实现公式1：均值/标准差计算新上下限
- [x] 3.3 实现公式2：中位数/IQR 计算新上下限
- [x] 3.4 将新上下限应用于数据，得到越界坐标列表

## 4. TSK 回写与输出

- [x] 4.1 根据越界坐标回写 TSK（Fail + Bin=Ink Bin）
- [x] 4.2 输出到 `DPAT_INK` 目录并追加 `_DPAT` 后缀
- [x] 4.3 保留 TSK_spec_2013 兼容性（文件头/扩展数据结构不破坏）

## 5. 基础验证与日志

- [x] 5.1 增加关键步骤日志（匹配、筛选数量、越界数量、输出路径）
- [ ] 5.2 使用 `docs/Csv格式.csv` 做一次冒烟验证并记录注意事项（已取消）
